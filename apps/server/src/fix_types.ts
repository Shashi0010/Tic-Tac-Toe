import { PlayerSymbol } from '@tic-tac-toe/shared';

// In RoomManager.ts, replace 'X' and 'O' with PlayerSymbol.X and PlayerSymbol.O if necessary,
// but since they are strings, the literal works too if the types match.
// For now, I'll just ensure the types are correctly used.
